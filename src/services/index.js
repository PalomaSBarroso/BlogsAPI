const userService = require('./user');
const categoryServices = require('./category');
const postService = require('./post');

module.exports = {
    userService,
    categoryServices,
    postService,
};